name: hermes-workspace-context
version: "1.1.1"
description: Apply project workspace contexts to selected gateway conversations.
author: Ryo Nakae
hooks:
  - pre_gateway_dispatch
provides_hooks:
  - pre_gateway_dispatch
